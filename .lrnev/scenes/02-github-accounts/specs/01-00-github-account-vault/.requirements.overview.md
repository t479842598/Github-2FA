React 管理台（账号/导入/设置三页）+ Express API。关键实现：crypto.js（scrypt/AES-GCM 零依赖）、totp.js（RFC 6238 通过官方向量）、parser.js（用户文本格式解析）、store.js（vault.json 字段级加密 + tokenVersion 吊销 + 备份恢复）。22 个单测全绿，e2e 含重启持久化/改密码/备份导入验证通过。
